---
interface Props {
  as?: "h1" | "h2" | "h3";
  class?: string;
}
const { as: Tag = "h2", class: className } = Astro.props;
---

<Tag
  class:list={[
    "text-3xl sm:text-4xl font-extrabold tracking-tighter mb-0",
    className,
  ]}
>
  <slot />
</Tag>
